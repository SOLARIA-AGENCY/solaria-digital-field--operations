# Core secrets (avoid special chars like ! @ # $ % in passwords for bash compatibility)
DB_PASSWORD=solaria2024
MYSQL_ROOT_PASSWORD=SolariaRoot2024
JWT_SECRET=solaria_jwt_secret_2024_min32chars_secure
MINIO_ROOT_USER=solaria_admin
MINIO_ROOT_PASSWORD=minio_secure_2024
OPENAI_API_KEY=

# App
NODE_ENV=production
PORT=3030

# Repo path for docs ingestion (adjust if different location)
REPO_PATH=/path/to/your/project
PROJECT_NAME=Akademate
PROJECT_TYPE=software
CI_MODE=true

# Dashboard login: carlosjperez / bypass
